<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
    <link rel="stylesheet" type="text/css" href="style.css">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>
<body class="bg-light">

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
    <div class="container-fluid">
        <a class="navbar-brand text-white" href="/">🐾 PetCare Journal</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-controls="navbarNav"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <% if (user) { %>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/view">View</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/register">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<!-- Welcome Content -->
<div class="container text-center mt-5">
    <h1>Welcome to PetCare Journal</h1>
    <p class="lead text-muted">
        A simple app to help you track your pet’s expenses, appointments, and daily care routines all in one place.
    </p>

    <% if (user) { %>
        <p class="mt-3">Welcome back, <strong><%= user.username %></strong>!</p>
        <a href="<%= user.role === 'admin' ? '/admin' : '/user' %>" class="btn btn-primary mt-3 px-4 py-2 fs-5">
            🐾 Go to Dashboard
        </a>
        <br>
        <a href="/logout" class="btn btn-outline-danger mt-3">Logout</a>
    <% } else { %>
        <a href="/login" class="btn btn-success mt-3 me-3 px-4 py-2 fs-5">Login</a>
        <a href="/register" class="btn btn-outline-secondary mt-3 px-4 py-2 fs-5">Register</a>
    <% } %>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>


